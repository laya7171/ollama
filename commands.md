### /show (shows all the specification in the model)

## /set (allows us to change the parameters like thingking, top k, temperature, etc)

### /set paramters(change the model parameters like p, k etc)

## /set parameter "top_p" to "0.9" (This will overwrite the model parameters)

## ways to use Ollama

# ollama lib

# REST API

# Langchain
